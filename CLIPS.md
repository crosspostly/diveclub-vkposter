# VK Клипы (Clips): загрузка в сообщество через shortVideo.create

## Коротко

Клип **в сообщество** загружается закрытым методом `shortVideo.create`
(его нет в dev.vk.com). Метод принимает **только токены официального
приложения ВК** — и их два способа получить:

1. **VK_CLIP_TOKEN** — vkhost.github.io → кнопка «vk.com» (проверено:
   метод существует, но живого теста с таким токеном не было).
2. **Web-токен из кук сессии** (✅ **рабочий, подтверждён живыми тестами**):
   каждая SPA-страница vk.ru, отданная авторизованной сессии, содержит
   `window.vk.webToken.access_token` — это официальный web-токен приложения.
   С ним `shortVideo.create` работает; нужен **редактор/админ группы**,
   иначе error 15 «Access denied».

С обычными токенами (VFeed и т.п.) и ключом сообщества метод не работает —
error 3 «Unknown method passed».

Обычный флоу `video.save` + `wall.post` клип в сообществе **не создаёт** —
проверено на 9:16, 3:4 и 9:16 Full HD (1080×1920). Но на **личную страницу
пользователя** то же видео через `video.save` + `wall.post` становится клипом.

Рабочий скрипт на web-токене — **`clip_web_upload.py`** (куки браузера,
без `VK_CLIP_TOKEN`); альтернатива с токеном vk.com — **`clip_upload.py`**.

## Наблюдения (живые тесты 2026-08-15/16)

### `video.save` + `wall.post` → в сообществе НЕ клип

| Формат | Видео (группа 96798355) | Пост | `video.get` |
|---|---|---|---|
| 9:16 720×1280, 3 сек | 456239211 | 8140 | `type=video` |
| 3:4 720×960, 4 сек | 456239212 | 8141 | `type=video` |
| 9:16 1080×1920 Full HD, 12 сек | 456239213 | 8142 | `type=video` |

Клип-полей в `video.get` нет; раздел `vk.com/clips-96798355` —
«У автора пока нет клипов». Формат/разрешение не влияют.

### А на страницу пользователя — клип

`video.save` **без** `group_id` (owner = 11813721) + `wall.post` на страницу
(пост **4916**, видео **456240323**, 3:4 720×960) → вложение
**`type: short_video`**, ролик появился в разделе «Клипы» пользователя.

**Вывод: решает адресат, а не формат.** На страницу пользователя ВК сам
превращает вертикаль в клип; в сообщество через `video.save` — нет.

### Доказательство: клипы в сообществе существуют (группа 92478300)

Группа «Быстрые рецепты на каждый день» (92478300) — **170 вертикальных
роликов 1080×1920 (9:16), 9–15 сек**, загруженных скриптами (март–июнь 2026).
Часть из них — **`type: short_video` (клипы)**: 456239326, 456239323,
456239322, 456239321. Особенности клипов: пустой `title`, поля
`repeat: 1` и `is_united_video: 1`, нет `player`/`direct_url` — это объекты
shortVideo-пайплайна, а не `video.save`. Пример: `vk.ru/clip-92478300_456239326`.

## Как это работает: `shortVideo.create`

Запрос: `POST api.vk.com/method/shortVideo.create` с параметрами:

| Параметр | Значение |
|---|---|
| `access_token` | **токен официального приложения** (vkhost «vk.com») |
| `group_id` | id сообщества (клип попадёт в раздел «Клипы» сообщества) |
| `wallpost` | `1` — сразу на стену, `0` — только в клипы |
| `description` | описание клипа (хештеги) |
| `file_size` | размер файла в байтах |
| `v` | 5.132–5.249 (можно 5.199) |

Ответ: `{owner_id, video_id, upload_url}`. Затем `POST` на `upload_url`:
multipart-поле **`file`**, user-agent **`vk-test-clip-upload 1`**.
Успех = HTTP 200 + тело `<retval>1</retval>`.

**Токен**: `vkhost.github.io` → кнопка **«vk.com»** (официальное приложение ВК).
Токен в `.env` → `VK_CLIP_TOKEN=<токен>` (в `.gitignore`, не коммитить).

**Ролик**: MP4, H.264, вертикаль 9:16 (1080×1920), 3 сек – 3 мин, ≤ 1 ГБ.
(В группе 92478300 все клипы именно 9:16 1080×1920 — 3:4 через
`shortVideo.create` не проверено.)

**Запуск**:
```bash
.venv/bin/python clip_upload.py clip.mp4 "Описание #хештег"        # wallpost=1
.venv/bin/python clip_upload.py clip.mp4 "" --wallpost 0           # только клип
```

Если токена vk.com нет — фолбэк: загрузка через веб-интерфейс (Playwright, ниже).

## ✅ Рабочий путь без VK_CLIP_TOKEN: web-токен из кук (доказан 2026-08-16)

Не нужен ни токен vkhost, ни Playwright — достаточно **кук живой веб-сессии
ВК** и прав **редактора/админа** на группу.

### Механизм

1. **Куки** сессии (JSON из браузера) → `GET https://vk.ru/clips-<group>` с
   заголовком `Cookie`. В HTML-ответе SPA есть
   `window.vk.webToken.access_token` (regex
   `"webToken":\s*\{\s*"access_token":"([^"]+)"`) — **официальный web-токен
   приложения** (220 символов, короткоживущий — брать перед каждым вызовом).
2. `shortVideo.create` этим токеном:
   - без прав на группу → **error 15 «Access denied»** (метод существует!);
   - с правами **редактора или админа** → `{owner_id, video_id, upload_url}`;
   - VFeed-токеном / ключом сообщества → error 3 (метода для них нет).
3. `POST` на `upload_url`: multipart-поле **`file`**, UA `vk-test-clip-upload 1`.
   Ответ — **два варианта** (оба принимаются):
   - старый: HTTP 200 + `<retval>1</retval>`;
   - новый: HTTP 200 + JSON `{"video_hash":..., "size":..., "owner_id":...,
     "video_id":...}`.
4. Клип в разделе «Клипы» сообщества: `type: short_video`, `owner_id`
   отрицательный, `is_united_video: 1`, `repeat: 1` — те же маркеры, что у
   клипов 92478300.

### Живое подтверждение (группа 96798355 «! тест»)

- Куки Pavel Shekhov (1102644572), **назначен редактором** группы:
  `shortVideo.create` сработал → клипы **456239214** (30 с) и **456239215**
  (12 с), оба `type: short_video`, `owner_id: -96798355`,
  `is_united_video: 1`, `repeat: 1`. Ссылки: `vk.ru/clip-96798355_456239214`,
  `vk.ru/clip-96798355_456239215`.
- **`video.get` НЕ показывает клипы**: в списке видеотеки сообщества (обеих
  групп) только `type: video`; клипы видны только в разделе «Клипы» (или по
  прямому id в `video.get` с `videos=-<gid>_<vid>` → `type: short_video`).
- **Клипы не появляются на стене**: ни `wallpost=1` при создании, ни
  отдельный `wall.post` с вложением клипа (ВК молча отбрасывает вложение —
  пост выходит с `attachments: []`). Подтверждает и группа 92478300: вся
  стена (249 постов) — ни одного клип-вложения; клипы живут только в
  разделе «Клипы».
- Обработка: 12-сек клип транскодируется быстро, 30-сек может висеть в
  `processing: 1` заметное время (очередь ВК).

### Скрипт: `clip_web_upload.py`

```bash
.venv/bin/python clip_web_upload.py --cookies vk_cookies.json --video clip.mp4 \
    --text "Описание #хештег" --group 96798355 --wallpost 1
```

- `--cookies` — JSON-файл кук vk.ru (формат EditThisCookie: `[{name, value, ...}]`).
- Куки = **живой доступ к аккаунту**: хранить только локально, в чат/терминал
  не выводить, в git не коммитить.

## Фолбэк: автоматизация загрузки клипов через Playwright

Если токена vk.com нет, клип можно загрузить через веб-интерфейс `vk.com`
с помощью Playwright: браузер повторяет действия человека в разделе «Клипы».

- **Плюсы**: не зависит от API и токенов; принимает всё, что принимает веб-форма.
- **Минусы**: хрупко к изменениям вёрстки, CAPTCHA/2FA, риск ограничений при
  частых загрузках, нужна живая сессия (cookies).

### Шаг 0. Установка

```bash
pip install playwright
playwright install chromium
```

### Шаг 1. Как получить cookies (живая сессия)

Надёжный способ — `storage_state` Playwright (хранит cookies **и** localStorage,
что важно для VK):

1. Один раз запустите браузер **в видимом окне** и войдите в ВК вручную
   (логин + 2FA/CAPTCHA может сделать только человек):

```python
from playwright.sync_api import sync_playwright

with sync_playwright() as p:
    browser = p.chromium.launch(headless=False)  # видимое окно
    ctx = browser.new_context()
    page = ctx.new_page()
    page.goto("https://vk.com/")
    input("Войдите в ВК в этом окне и нажмите Enter...")
    ctx.storage_state(path="vk_state.json")   # сессия сохранена
    browser.close()
```

2. Все дальнейшие запуски — headless, с подгрузкой сессии:

```python
ctx = browser.new_context(storage_state="vk_state.json")
```

Альтернатива: экспорт cookies расширением EditThisCookie, но `storage_state`
надёжнее.

> **Безопасность**: `vk_state.json` = живой доступ к аккаунту. Храните только
> локально, добавьте в `.gitignore`, не коммитьте и не передавайте никому.
> Сессия протухает — периодически обновляйте.

### Шаг 2. Как найти селекторы (исследовать код страницы)

Не гадайте — исследуйте. Два рабочих способа:

**A. Playwright Codegen (быстрее всего):**

```bash
playwright codegen https://vk.com/clips-96798355
```

Откроется браузер + инспектор. Каждое ваше действие записывается в код
с селекторами: кликните «Опубликовать», выберите файл, нажмите публикацию —
и скопируйте готовые селекторы.

**B. DevTools вручную:**

1. Откройте `vk.com/clips-<id>` → F12 → вкладка Elements.
2. Наведите курсор на кнопку «Опубликовать», поле описания, `input[type=file]` —
   увидите тег, `id`, `data-testid`, классы.
3. Проверьте уникальность селектора в консоли:
   `document.querySelectorAll('ваш-селектор').length` должно быть `1`.

Приоритет надёжности селекторов:
1. Роль/текст: `get_by_role("button", name="Опубликовать")`, `get_by_text(...)`;
2. `data-testid` / стабильные `id`;
3. CSS-классы — **в последнюю очередь** (VK часто их меняет).

Особенность VK: `input[type=file]` скрытый — Playwright умеет
`set_input_files` по нему, даже когда он невидим.

### Шаг 3. Каркас скрипта (предложение, в репо не реализовано)

```python
from playwright.sync_api import sync_playwright

GROUP_ID = "96798355"
VIDEO = "clip.mp4"  # вертикальный MP4 9:16, 3 сек–3 мин, ≤1 ГБ

with sync_playwright() as p:
    browser = p.chromium.launch(headless=True)
    ctx = browser.new_context(storage_state="vk_state.json")  # шаг 1
    page = ctx.new_page()
    page.goto(f"https://vk.com/clips-{GROUP_ID}", wait_until="networkidle")

    # 1. Кнопка «Опубликовать» на странице клипов сообщества
    page.get_by_text("Опубликовать", exact=True).first.click()

    # 2. Скрытый input[type=file] в модалке загрузки
    page.locator("input[type=file]").set_input_files(VIDEO)

    # 3. Дождаться обработки файла (подберите реальный селектор превью/описания)
    page.wait_for_timeout(15000)

    # 4. Описание (хештеги)
    desc = page.get_by_placeholder("Добавьте описание").first
    if desc.is_visible():
        desc.fill("Клип #тест")

    # 5. Публикация
    page.get_by_text("Опубликовать", exact=True).last.click()

    page.wait_for_timeout(3000)
    print("Готово. Проверьте vk.com/clips-96798355")
    browser.close()
```

Это **каркас**: селекторы и тайминги определяйте на шаге 2 под текущую вёрстку.

### Шаг 4. Подводные камни

- **CAPTCHA / 2FA**: при подозрении ВК попросит подтверждение — headless падает.
  Лечение: реже загружать, имитировать человека (паузы, курсор), при
  необходимости ручное подтверждение в headed-режиме.
- **Детект headless**: `headless=False` + реальный user-agent; при массовых
  загрузках — прокси и разные сессии.
- **Протухание сессии**: перед загрузкой проверяйте, что открыта страница
  клипов, а не форма логина.
- **Изменение вёрстки**: VK меняет классы/структуру — перепроверяйте селекторы,
  не закладывайтесь на классы без проверки.
- **Частые загрузки** → риск ограничений/бана; соблюдайте паузы.
- **Локализация**: текст кнопок зависит от языка интерфейса — предпочитайте
  `data-testid` текстовым селекторам.

## Статус

- Наблюдения подтверждены живыми тестами 2026-08-15/16 (группа `96798355`,
  посты 8138/8140/8141/8142, видео 456239208–456239213; страница пользователя
  11813721 — пост 4916, видео 456240323; группа 92478300 — клипы
  456239326/323/322/321).
- **✅ `shortVideo.create` через web-токен из кук — рабочий путь в
  сообщество, проверен живыми клипами 456239214/456239215 (группа 96798355,
  права редактора).** Скрипт — `clip_web_upload.py`.
- `clip_upload.py` (VK_CLIP_TOKEN от vkhost «vk.com») — живой тест всё ещё
  не проведён (такого токена нет); механизм тот же `shortVideo.create`.
- Клипы НЕ показываются в видеотеке (`video.get`) и НЕ попадают на стену
  (ни `wallpost=1`, ни `wall.post` с вложением).
- Playwright-инструкция — **фолбэк**: код в репо не добавлен и не протестирован.
